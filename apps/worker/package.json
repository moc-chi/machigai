{
  "name": "@machigai/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@machigai/drawing": "*",
    "@machigai/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250829.0",
    "wrangler": "^4.33.1"
  }
}
